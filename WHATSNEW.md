# 更新说明

## v2.0.0 · 2026-09-17

### ✨ 新增：HTML 需求文档支持

需求文档是**单文件 HTML** 时，不再需要转 PDF、转 PPT 或截图，直接把路径交给 skill 即可。

#### 为什么单独做一条路径

需求 HTML 通常是自包含单文件：正文只有一两万字符，但 CSS 加内嵌 base64 原型图会把文件撑到几 MB。实测一份 MP3 Cutter 需求 **2.7MB，正文仅 1.2 万字符**，其余 99% 是样式和 24 张 base64 图。

直接 Read 会一次性撑爆上下文，**任务当场作废且没有任何补救办法**。SKILL.md 已把「禁止直接 Read HTML 需求文档」列为关键规则 6。

#### 怎么用

```bash
# 1. 先列章节目录，决定这次做哪几节
python scripts/extract_html.py --input 需求.html --info

# 输出：
# 📑 章节目录（分节依据：<section> 标签，共 7 节）：
#     1. §1 需求概述          约 1011 字符 | 图 0 张
#     2. §2 全局宽布局框架     约 7469 字符 | 图 20 张
#     ...

# 2. 按章节提取正文与内嵌原型图
python scripts/extract_html.py --input 需求.html --sections 1-3 --outdir html_output
```

产物：

- `html_output/prd_s1-2-3.md` —— 只含选中章节的正文
- `html_output/images/img_NN.jpg` —— 该章节内的原型图
- 正文里图片原位留 `[[IMAGE: images/img_21.jpg]] alt=居中弹窗` 锚点，文件末尾附图片清单

#### 相比截图路线的优势

| | HTML 提取 | PPT / PDF 截图 |
|---|---|---|
| 断点数值、规则表、公式 | 直读文本，**无损** | 从截图里看，可能看错数字 |
| 表格 | 自动转 Markdown | 靠视觉还原 |
| 上下文占用 | 只读选中章节 | 每页一张图 |

#### 分批节奏与 PPT 路径一致

`--sections` 就是 `--slides` 的等价物，「先做 1-3 节，再做 4-6 节」的节奏原样适用，只是把「页」换成「章节」。图片编号是**全文全局**的，不同批次的 md 文件名带章节后缀，**多轮提取到同一目录不会互相覆盖**。

#### 脚本告警要当真

正文过短（<500 字符）多半是 JS 动态渲染页面，静态解析拿不到内容；未解析到表格可能是 div 伪表格；有外链图片未取回会在正文标 `[[IMAGE-EXTERNAL]]`——这几种情况都要停下来告诉用户，别假装看过。

---

### 🐛 修复：新增行标红在部分阅读器里看不见

整行标红的新增用例，红色写进 `styles.xml` 时 alpha 通道为 `00`（完全透明），而原先的修复函数只处理 `sheet1.xml` 的富文本，**修不到它**——表现为内容明明在，看着却像空白行。

现已在 zip 层一并修正。非标准格式走的临时脚本模板（SKILL.md Step 6）也同步补上了这段。

---

### 🆕 新增：`--sheet-name` 参数

```bash
python scripts/generate.py --new --output out.xlsx --changes changes.json \
  --sheet-name "target36横屏适配"
```

两种模式通用，默认仍是 `Sheet`。自动做 Excel 合法性清洗（≤31 字符，剔除 `: \ / ? * [ ]`）。

> ⚠️ sheet 改名和颜色修补都必须在 zip 层完成。生成后再用 openpyxl 打开改名并 `save()`，会把颜色修补原样写回去，标红重新变透明。

---

### 🗑 移除

`references/XRecorder/` 偏好文件。偏好改由 tester-app 知识库通过显式路径传入，skill 不再按产品名查找 `references/` 目录（该机制自 v1.9.6 起生效）。

---

## 历史版本

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.9.6 | 2026-07-27 | 恢复 tester-app 显式传偏好路径机制 |
| v1.9.5 | 2026-06-25 | `extract_pptx.py` 重命名为 `extract_prd.py` |
| v1.9.4 | 2026-06-25 | 新增弹窗关闭交互、页面模块 UI 用例强制覆盖规则 |
| v1.9.3 | 2026-06-24 | 提取脚本支持 PDF 直接输入，跳过 LibreOffice |
| v1.9.2 | 2026-06-24 | 移除 LibreOffice 系统依赖（改用 python-pptx） |
| v1.9.1 | 2026-06-05 | 修复 Windows 上 LibreOffice 弹窗 |
| v1.9.0 | 2026-05-26 | 新增模块-用例树形结构输出与用户确认环节 |
| v1.8.0 | 2026-05-21 | 新增全新需求路径（无 CSV 时全量生成） |
| v1.6.0 | 2026-05-13 | CSV 优先查找规则、UI 改版强制检查、改名感知定位 |
| v1.5.0 | 2026-05-12 | modified 定位由行号改为 module+case，不受新行插入影响 |
| v1.4.0 | 2026-05-11 | 版本号写进 SKILL.md 正文，便于模型读取 |
| v1.3.3 | 2026-05-11 | 新增 Step 4.5 覆盖率复核 |
| v1.3.2 | 2026-05-11 | 新增 PPTX 页面提取脚本 |
| v1.3.1 | 2026-04-02 | 修复 CSV 中文乱码（显式 utf-8） |
| v1.3.0 | 2026-04-02 | Before/After 原型对比与元素移除处理 |
| v1.2.1 | 2026-04-02 | 关键步骤要求可见的执行输出 |
| v1.2.0 | 2026-04-02 | 需求截图必须同时分析文字与原型图 |
| v1.1.0 | 2026-04-02 | 新增 Step 2：先读 CSV 建立需求基线 |
| v1.0.0 | 2026-04-02 | 首个版本 |
